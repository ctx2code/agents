# Agent Rules

## Commit Message Guidelines

- Add AIs to the co-authors in commit message if it contains AI-generated code.
- Add AIs in the form of `Co-Authored-By: NAME <NAME@noreply.local>`.
  - Where `NAME` is the name-dash-version of the language model, eg. `gpt-4.1`.
